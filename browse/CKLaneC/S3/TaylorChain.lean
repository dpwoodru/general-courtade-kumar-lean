import GeneralCK.Certificates.E8TAxisDeltaDirectionalJet

/-!
# Lane C3 (snapshot copied into Lane C, namespace CKLaneC.S3) — Lagrange–Taylor remainders for derivative chains on `[0,1]`

For a chain `f₀, …, fₙ` with `fᵢ' = fᵢ₊₁` on `[0,1]` there is `θ ∈ (0,1)` with
`f₀ 1 = Σ_{i<n} fᵢ 0 / i! + fₙ θ / n!` (orders `n = 1, …, 5`), by iterated Rolle.
(Generated by tools/gen_taylor_chain.py; proof pattern from
`GeneralCK.Certificates.E8CompactAnchorTaylor3.exists_remainder`.)
-/

set_option autoImplicit false
set_option relaxedAutoImplicit false
set_option linter.unusedSimpArgs false

namespace CKLaneC.S3.TaylorChain

open Set

/-- One Rolle step on a sub-interval `[0,b] ⊆ [0,1]`. -/
theorem rolle_step {g dg : ℝ → ℝ} {b : ℝ}
    (hb : 0 < b) (hb1 : b ≤ 1)
    (hd : ∀ u ∈ Icc (0 : ℝ) 1, HasDerivAt g (dg u) u)
    (heq : g 0 = g b) : ∃ c ∈ Ioo (0 : ℝ) b, dg c = 0 := by
  apply exists_hasDerivAt_eq_zero hb
  · intro u hu
    exact (hd u ⟨hu.1, hu.2.trans hb1⟩).continuousAt.continuousWithinAt
  · exact heq
  · intro u hu
    exact hd u ⟨hu.1.le, hu.2.le.trans hb1⟩

/-- Order 1. -/
theorem exists_remainder1 {f f1 : ℝ → ℝ}
    (h0 : ∀ u ∈ Icc (0 : ℝ) 1, HasDerivAt f (f1 u) u) :
    ∃ u ∈ Ioo (0 : ℝ) 1, f 1 = f 0 + f1 u := by
  let R := 1 * (f 1 - (f 0))
  let g0 : ℝ → ℝ := fun u => f u - (f 0 + R * u)
  have hd0 : ∀ u ∈ Icc (0 : ℝ) 1, HasDerivAt g0 (f1 u - R) u := by
    intro u hu
    have hd := (h0 u hu).sub ((hasDerivAt_const u (f 0)).add ((hasDerivAt_id u).const_mul R))
    convert! hd using 1 <;>
      (try simp only [g0, Pi.sub_apply, Pi.add_apply, Pi.pow_apply, id_eq]) <;>
      first
      | (funext x; simp only [Pi.sub_apply, Pi.add_apply, Pi.pow_apply, id_eq]; ring)
      | ring
      | (simp; ring)
      | simp
  obtain ⟨u1, hu1, he1⟩ := rolle_step (by norm_num : (0 : ℝ) < 1) le_rfl hd0
    (by simp only [g0, R]; ring)
  refine ⟨u1, ⟨hu1.1, hu1.2⟩, ?_⟩
  have hR : f1 u1 = R := by linarith
  rw [hR]
  simp only [R]
  ring

/-- Order 2. -/
theorem exists_remainder2 {f f1 f2 : ℝ → ℝ}
    (h0 : ∀ u ∈ Icc (0 : ℝ) 1, HasDerivAt f (f1 u) u)
    (h1 : ∀ u ∈ Icc (0 : ℝ) 1, HasDerivAt f1 (f2 u) u) :
    ∃ u ∈ Ioo (0 : ℝ) 1, f 1 = f 0 + f1 0 + f2 u / 2 := by
  let R := 2 * (f 1 - (f 0 + f1 0))
  let g0 : ℝ → ℝ := fun u => f u - (f 0 + f1 0 * u + R * u ^ 2 / 2)
  let g1 : ℝ → ℝ := fun u => f1 u - (f1 0 + R * u)
  have hd0 : ∀ u ∈ Icc (0 : ℝ) 1, HasDerivAt g0 (g1 u) u := by
    intro u hu
    have hd := (h0 u hu).sub (((hasDerivAt_const u (f 0)).add ((hasDerivAt_id u).const_mul (f1 0))).add (((hasDerivAt_id u).pow 2).const_mul (R / 2)))
    convert! hd using 1 <;>
      (try simp only [g0, g1, Pi.sub_apply, Pi.add_apply, Pi.pow_apply, id_eq]) <;>
      first
      | (funext x; simp only [Pi.sub_apply, Pi.add_apply, Pi.pow_apply, id_eq]; ring)
      | ring
      | (simp; ring)
      | simp
  have hd1 : ∀ u ∈ Icc (0 : ℝ) 1, HasDerivAt g1 (f2 u - R) u := by
    intro u hu
    have hd := (h1 u hu).sub ((hasDerivAt_const u (f1 0)).add ((hasDerivAt_id u).const_mul R))
    convert! hd using 1 <;>
      (try simp only [g1, Pi.sub_apply, Pi.add_apply, Pi.pow_apply, id_eq]) <;>
      first
      | (funext x; simp only [Pi.sub_apply, Pi.add_apply, Pi.pow_apply, id_eq]; ring)
      | ring
      | (simp; ring)
      | simp
  obtain ⟨u1, hu1, he1⟩ := rolle_step (by norm_num : (0 : ℝ) < 1) le_rfl hd0
    (by simp only [g0, R]; ring)
  obtain ⟨u2, hu2, he2⟩ := rolle_step hu1.1 hu1.2.le hd1
    (by rw [he1]; simp [g1])
  refine ⟨u2, ⟨hu2.1, hu2.2.trans (hu1.2)⟩, ?_⟩
  have hR : f2 u2 = R := by linarith
  rw [hR]
  simp only [R]
  ring

/-- Order 3. -/
theorem exists_remainder3 {f f1 f2 f3 : ℝ → ℝ}
    (h0 : ∀ u ∈ Icc (0 : ℝ) 1, HasDerivAt f (f1 u) u)
    (h1 : ∀ u ∈ Icc (0 : ℝ) 1, HasDerivAt f1 (f2 u) u)
    (h2 : ∀ u ∈ Icc (0 : ℝ) 1, HasDerivAt f2 (f3 u) u) :
    ∃ u ∈ Ioo (0 : ℝ) 1, f 1 = f 0 + f1 0 + f2 0 / 2 + f3 u / 6 := by
  let R := 6 * (f 1 - (f 0 + f1 0 + f2 0 / 2))
  let g0 : ℝ → ℝ := fun u => f u - (f 0 + f1 0 * u + f2 0 * u ^ 2 / 2 + R * u ^ 3 / 6)
  let g1 : ℝ → ℝ := fun u => f1 u - (f1 0 + f2 0 * u + R * u ^ 2 / 2)
  let g2 : ℝ → ℝ := fun u => f2 u - (f2 0 + R * u)
  have hd0 : ∀ u ∈ Icc (0 : ℝ) 1, HasDerivAt g0 (g1 u) u := by
    intro u hu
    have hd := (h0 u hu).sub ((((hasDerivAt_const u (f 0)).add ((hasDerivAt_id u).const_mul (f1 0))).add (((hasDerivAt_id u).pow 2).const_mul (f2 0 / 2))).add (((hasDerivAt_id u).pow 3).const_mul (R / 6)))
    convert! hd using 1 <;>
      (try simp only [g0, g1, Pi.sub_apply, Pi.add_apply, Pi.pow_apply, id_eq]) <;>
      first
      | (funext x; simp only [Pi.sub_apply, Pi.add_apply, Pi.pow_apply, id_eq]; ring)
      | ring
      | (simp; ring)
      | simp
  have hd1 : ∀ u ∈ Icc (0 : ℝ) 1, HasDerivAt g1 (g2 u) u := by
    intro u hu
    have hd := (h1 u hu).sub (((hasDerivAt_const u (f1 0)).add ((hasDerivAt_id u).const_mul (f2 0))).add (((hasDerivAt_id u).pow 2).const_mul (R / 2)))
    convert! hd using 1 <;>
      (try simp only [g1, g2, Pi.sub_apply, Pi.add_apply, Pi.pow_apply, id_eq]) <;>
      first
      | (funext x; simp only [Pi.sub_apply, Pi.add_apply, Pi.pow_apply, id_eq]; ring)
      | ring
      | (simp; ring)
      | simp
  have hd2 : ∀ u ∈ Icc (0 : ℝ) 1, HasDerivAt g2 (f3 u - R) u := by
    intro u hu
    have hd := (h2 u hu).sub ((hasDerivAt_const u (f2 0)).add ((hasDerivAt_id u).const_mul R))
    convert! hd using 1 <;>
      (try simp only [g2, Pi.sub_apply, Pi.add_apply, Pi.pow_apply, id_eq]) <;>
      first
      | (funext x; simp only [Pi.sub_apply, Pi.add_apply, Pi.pow_apply, id_eq]; ring)
      | ring
      | (simp; ring)
      | simp
  obtain ⟨u1, hu1, he1⟩ := rolle_step (by norm_num : (0 : ℝ) < 1) le_rfl hd0
    (by simp only [g0, R]; ring)
  obtain ⟨u2, hu2, he2⟩ := rolle_step hu1.1 hu1.2.le hd1
    (by rw [he1]; simp [g1])
  obtain ⟨u3, hu3, he3⟩ := rolle_step hu2.1 (hu2.2.trans (hu1.2)).le hd2
    (by rw [he2]; simp [g2])
  refine ⟨u3, ⟨hu3.1, hu3.2.trans (hu2.2.trans (hu1.2))⟩, ?_⟩
  have hR : f3 u3 = R := by linarith
  rw [hR]
  simp only [R]
  ring

/-- Order 4. -/
theorem exists_remainder4 {f f1 f2 f3 f4 : ℝ → ℝ}
    (h0 : ∀ u ∈ Icc (0 : ℝ) 1, HasDerivAt f (f1 u) u)
    (h1 : ∀ u ∈ Icc (0 : ℝ) 1, HasDerivAt f1 (f2 u) u)
    (h2 : ∀ u ∈ Icc (0 : ℝ) 1, HasDerivAt f2 (f3 u) u)
    (h3 : ∀ u ∈ Icc (0 : ℝ) 1, HasDerivAt f3 (f4 u) u) :
    ∃ u ∈ Ioo (0 : ℝ) 1, f 1 = f 0 + f1 0 + f2 0 / 2 + f3 0 / 6 + f4 u / 24 := by
  let R := 24 * (f 1 - (f 0 + f1 0 + f2 0 / 2 + f3 0 / 6))
  let g0 : ℝ → ℝ := fun u => f u - (f 0 + f1 0 * u + f2 0 * u ^ 2 / 2 + f3 0 * u ^ 3 / 6 + R * u ^ 4 / 24)
  let g1 : ℝ → ℝ := fun u => f1 u - (f1 0 + f2 0 * u + f3 0 * u ^ 2 / 2 + R * u ^ 3 / 6)
  let g2 : ℝ → ℝ := fun u => f2 u - (f2 0 + f3 0 * u + R * u ^ 2 / 2)
  let g3 : ℝ → ℝ := fun u => f3 u - (f3 0 + R * u)
  have hd0 : ∀ u ∈ Icc (0 : ℝ) 1, HasDerivAt g0 (g1 u) u := by
    intro u hu
    have hd := (h0 u hu).sub (((((hasDerivAt_const u (f 0)).add ((hasDerivAt_id u).const_mul (f1 0))).add (((hasDerivAt_id u).pow 2).const_mul (f2 0 / 2))).add (((hasDerivAt_id u).pow 3).const_mul (f3 0 / 6))).add (((hasDerivAt_id u).pow 4).const_mul (R / 24)))
    convert! hd using 1 <;>
      (try simp only [g0, g1, Pi.sub_apply, Pi.add_apply, Pi.pow_apply, id_eq]) <;>
      first
      | (funext x; simp only [Pi.sub_apply, Pi.add_apply, Pi.pow_apply, id_eq]; ring)
      | ring
      | (simp; ring)
      | simp
  have hd1 : ∀ u ∈ Icc (0 : ℝ) 1, HasDerivAt g1 (g2 u) u := by
    intro u hu
    have hd := (h1 u hu).sub ((((hasDerivAt_const u (f1 0)).add ((hasDerivAt_id u).const_mul (f2 0))).add (((hasDerivAt_id u).pow 2).const_mul (f3 0 / 2))).add (((hasDerivAt_id u).pow 3).const_mul (R / 6)))
    convert! hd using 1 <;>
      (try simp only [g1, g2, Pi.sub_apply, Pi.add_apply, Pi.pow_apply, id_eq]) <;>
      first
      | (funext x; simp only [Pi.sub_apply, Pi.add_apply, Pi.pow_apply, id_eq]; ring)
      | ring
      | (simp; ring)
      | simp
  have hd2 : ∀ u ∈ Icc (0 : ℝ) 1, HasDerivAt g2 (g3 u) u := by
    intro u hu
    have hd := (h2 u hu).sub (((hasDerivAt_const u (f2 0)).add ((hasDerivAt_id u).const_mul (f3 0))).add (((hasDerivAt_id u).pow 2).const_mul (R / 2)))
    convert! hd using 1 <;>
      (try simp only [g2, g3, Pi.sub_apply, Pi.add_apply, Pi.pow_apply, id_eq]) <;>
      first
      | (funext x; simp only [Pi.sub_apply, Pi.add_apply, Pi.pow_apply, id_eq]; ring)
      | ring
      | (simp; ring)
      | simp
  have hd3 : ∀ u ∈ Icc (0 : ℝ) 1, HasDerivAt g3 (f4 u - R) u := by
    intro u hu
    have hd := (h3 u hu).sub ((hasDerivAt_const u (f3 0)).add ((hasDerivAt_id u).const_mul R))
    convert! hd using 1 <;>
      (try simp only [g3, Pi.sub_apply, Pi.add_apply, Pi.pow_apply, id_eq]) <;>
      first
      | (funext x; simp only [Pi.sub_apply, Pi.add_apply, Pi.pow_apply, id_eq]; ring)
      | ring
      | (simp; ring)
      | simp
  obtain ⟨u1, hu1, he1⟩ := rolle_step (by norm_num : (0 : ℝ) < 1) le_rfl hd0
    (by simp only [g0, R]; ring)
  obtain ⟨u2, hu2, he2⟩ := rolle_step hu1.1 hu1.2.le hd1
    (by rw [he1]; simp [g1])
  obtain ⟨u3, hu3, he3⟩ := rolle_step hu2.1 (hu2.2.trans (hu1.2)).le hd2
    (by rw [he2]; simp [g2])
  obtain ⟨u4, hu4, he4⟩ := rolle_step hu3.1 (hu3.2.trans (hu2.2.trans (hu1.2))).le hd3
    (by rw [he3]; simp [g3])
  refine ⟨u4, ⟨hu4.1, hu4.2.trans (hu3.2.trans (hu2.2.trans (hu1.2)))⟩, ?_⟩
  have hR : f4 u4 = R := by linarith
  rw [hR]
  simp only [R]
  ring

/-- Order 5. -/
theorem exists_remainder5 {f f1 f2 f3 f4 f5 : ℝ → ℝ}
    (h0 : ∀ u ∈ Icc (0 : ℝ) 1, HasDerivAt f (f1 u) u)
    (h1 : ∀ u ∈ Icc (0 : ℝ) 1, HasDerivAt f1 (f2 u) u)
    (h2 : ∀ u ∈ Icc (0 : ℝ) 1, HasDerivAt f2 (f3 u) u)
    (h3 : ∀ u ∈ Icc (0 : ℝ) 1, HasDerivAt f3 (f4 u) u)
    (h4 : ∀ u ∈ Icc (0 : ℝ) 1, HasDerivAt f4 (f5 u) u) :
    ∃ u ∈ Ioo (0 : ℝ) 1, f 1 = f 0 + f1 0 + f2 0 / 2 + f3 0 / 6 + f4 0 / 24 + f5 u / 120 := by
  let R := 120 * (f 1 - (f 0 + f1 0 + f2 0 / 2 + f3 0 / 6 + f4 0 / 24))
  let g0 : ℝ → ℝ := fun u => f u - (f 0 + f1 0 * u + f2 0 * u ^ 2 / 2 + f3 0 * u ^ 3 / 6 + f4 0 * u ^ 4 / 24 + R * u ^ 5 / 120)
  let g1 : ℝ → ℝ := fun u => f1 u - (f1 0 + f2 0 * u + f3 0 * u ^ 2 / 2 + f4 0 * u ^ 3 / 6 + R * u ^ 4 / 24)
  let g2 : ℝ → ℝ := fun u => f2 u - (f2 0 + f3 0 * u + f4 0 * u ^ 2 / 2 + R * u ^ 3 / 6)
  let g3 : ℝ → ℝ := fun u => f3 u - (f3 0 + f4 0 * u + R * u ^ 2 / 2)
  let g4 : ℝ → ℝ := fun u => f4 u - (f4 0 + R * u)
  have hd0 : ∀ u ∈ Icc (0 : ℝ) 1, HasDerivAt g0 (g1 u) u := by
    intro u hu
    have hd := (h0 u hu).sub ((((((hasDerivAt_const u (f 0)).add ((hasDerivAt_id u).const_mul (f1 0))).add (((hasDerivAt_id u).pow 2).const_mul (f2 0 / 2))).add (((hasDerivAt_id u).pow 3).const_mul (f3 0 / 6))).add (((hasDerivAt_id u).pow 4).const_mul (f4 0 / 24))).add (((hasDerivAt_id u).pow 5).const_mul (R / 120)))
    convert! hd using 1 <;>
      (try simp only [g0, g1, Pi.sub_apply, Pi.add_apply, Pi.pow_apply, id_eq]) <;>
      first
      | (funext x; simp only [Pi.sub_apply, Pi.add_apply, Pi.pow_apply, id_eq]; ring)
      | ring
      | (simp; ring)
      | simp
  have hd1 : ∀ u ∈ Icc (0 : ℝ) 1, HasDerivAt g1 (g2 u) u := by
    intro u hu
    have hd := (h1 u hu).sub (((((hasDerivAt_const u (f1 0)).add ((hasDerivAt_id u).const_mul (f2 0))).add (((hasDerivAt_id u).pow 2).const_mul (f3 0 / 2))).add (((hasDerivAt_id u).pow 3).const_mul (f4 0 / 6))).add (((hasDerivAt_id u).pow 4).const_mul (R / 24)))
    convert! hd using 1 <;>
      (try simp only [g1, g2, Pi.sub_apply, Pi.add_apply, Pi.pow_apply, id_eq]) <;>
      first
      | (funext x; simp only [Pi.sub_apply, Pi.add_apply, Pi.pow_apply, id_eq]; ring)
      | ring
      | (simp; ring)
      | simp
  have hd2 : ∀ u ∈ Icc (0 : ℝ) 1, HasDerivAt g2 (g3 u) u := by
    intro u hu
    have hd := (h2 u hu).sub ((((hasDerivAt_const u (f2 0)).add ((hasDerivAt_id u).const_mul (f3 0))).add (((hasDerivAt_id u).pow 2).const_mul (f4 0 / 2))).add (((hasDerivAt_id u).pow 3).const_mul (R / 6)))
    convert! hd using 1 <;>
      (try simp only [g2, g3, Pi.sub_apply, Pi.add_apply, Pi.pow_apply, id_eq]) <;>
      first
      | (funext x; simp only [Pi.sub_apply, Pi.add_apply, Pi.pow_apply, id_eq]; ring)
      | ring
      | (simp; ring)
      | simp
  have hd3 : ∀ u ∈ Icc (0 : ℝ) 1, HasDerivAt g3 (g4 u) u := by
    intro u hu
    have hd := (h3 u hu).sub (((hasDerivAt_const u (f3 0)).add ((hasDerivAt_id u).const_mul (f4 0))).add (((hasDerivAt_id u).pow 2).const_mul (R / 2)))
    convert! hd using 1 <;>
      (try simp only [g3, g4, Pi.sub_apply, Pi.add_apply, Pi.pow_apply, id_eq]) <;>
      first
      | (funext x; simp only [Pi.sub_apply, Pi.add_apply, Pi.pow_apply, id_eq]; ring)
      | ring
      | (simp; ring)
      | simp
  have hd4 : ∀ u ∈ Icc (0 : ℝ) 1, HasDerivAt g4 (f5 u - R) u := by
    intro u hu
    have hd := (h4 u hu).sub ((hasDerivAt_const u (f4 0)).add ((hasDerivAt_id u).const_mul R))
    convert! hd using 1 <;>
      (try simp only [g4, Pi.sub_apply, Pi.add_apply, Pi.pow_apply, id_eq]) <;>
      first
      | (funext x; simp only [Pi.sub_apply, Pi.add_apply, Pi.pow_apply, id_eq]; ring)
      | ring
      | (simp; ring)
      | simp
  obtain ⟨u1, hu1, he1⟩ := rolle_step (by norm_num : (0 : ℝ) < 1) le_rfl hd0
    (by simp only [g0, R]; ring)
  obtain ⟨u2, hu2, he2⟩ := rolle_step hu1.1 hu1.2.le hd1
    (by rw [he1]; simp [g1])
  obtain ⟨u3, hu3, he3⟩ := rolle_step hu2.1 (hu2.2.trans (hu1.2)).le hd2
    (by rw [he2]; simp [g2])
  obtain ⟨u4, hu4, he4⟩ := rolle_step hu3.1 (hu3.2.trans (hu2.2.trans (hu1.2))).le hd3
    (by rw [he3]; simp [g3])
  obtain ⟨u5, hu5, he5⟩ := rolle_step hu4.1 (hu4.2.trans (hu3.2.trans (hu2.2.trans (hu1.2)))).le hd4
    (by rw [he4]; simp [g4])
  refine ⟨u5, ⟨hu5.1, hu5.2.trans (hu4.2.trans (hu3.2.trans (hu2.2.trans (hu1.2))))⟩, ?_⟩
  have hR : f5 u5 = R := by linarith
  rw [hR]
  simp only [R]
  ring

#print axioms exists_remainder1
#print axioms exists_remainder2
#print axioms exists_remainder3
#print axioms exists_remainder4
#print axioms exists_remainder5

end CKLaneC.S3.TaylorChain
